package com.shortener.challenge.application.shorturl.retrieve.get.id;

import com.shortener.challenge.application.UseCase;
import java.util.UUID;

public abstract class GetByIdShortURLUseCase
    extends UseCase<UUID, GetByIdShortURLOutput> {
}
